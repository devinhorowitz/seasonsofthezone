<w>
<!--
    Seasons of the Zone - the PDA page.

    Textureless except for two things this mod already ships and therefore cannot be
    missing: the year dial (ui_seasons_dial_NN, 384x384) and the season accent bar
    (ui_season_hdr_<season>, 256x16 - the same strip MCM puts atop each season page).

    Everything else is text. The PDA frame textures the other tab-adding mods reach for -
    ui_inGame2_pda_buttons_background, ui_inGame2_pda_line_vertical - are declared in NO
    texture_descr anywhere in this install, which is why opening those pages logs
    "Can't find texture". Borrowing them would have inherited the same fault.

    Colours follow the Anomaly PDA convention: amber 238,196,112 for headings, light grey
    for body, dim grey for things that are off. Fonts are letterica, as everywhere else.
-->
    <background x="28" y="106" width="764" height="603" stretch="1"/>

    <!-- season name, then the accent bar in that season's colour -->
    <header x="48" y="120" width="470" height="28">
        <text font="letterica18" r="238" g="196" b="112" align="l" vert_align="c" complex_mode="1"/>
    </header>

    <bar x="48" y="150" width="470" height="8" stretch="1"/>

    <subheader x="48" y="162" width="470" height="20">
        <text font="letterica16" r="170" g="170" b="170" align="l" vert_align="c" complex_mode="1"/>
    </subheader>

    <!-- the turning: what the year does next -->
    <turning x="48" y="186" width="470" height="22">
        <text font="letterica16" r="255" g="255" b="255" align="l" vert_align="c" complex_mode="1"/>
    </turning>

    <!-- shown only when today is one of the marked days -->
    <marked x="48" y="210" width="470" height="22">
        <text font="letterica18" r="238" g="196" b="112" align="l" vert_align="c" complex_mode="1"/>
    </marked>

    <!-- the year dial; square, width corrected for aspect from script -->
    <dial x="556" y="130" width="200" height="200" stretch="1"/>

    <dial_caption x="540" y="336" width="232" height="18">
        <text font="letterica16" r="170" g="170" b="170" align="c" vert_align="c" complex_mode="1"/>
    </dial_caption>

    <scroll x="48" y="242" width="480" height="452" always_show_scroll="0" stretch="1"/>

    <!-- the back button; its texture is set from script, as MAC does for its tiles -->
    <back x="740" y="652" width="34" height="34" stretch="1"/>

    <!-- rows are cloned from these at runtime. row_holder carries the columns: one text
         widget per column at a fixed x, because letterica is proportional and padded
         strings do not line up. -->
    <row_holder x="0" y="0" width="470" height="18"/>

    <row_head x="0" y="0" width="460" height="26">
        <text font="letterica18" r="238" g="196" b="112" align="l" vert_align="c" complex_mode="1"/>
    </row_head>

    <row_line x="0" y="0" width="460" height="18">
        <text font="letterica16" r="200" g="200" b="200" align="l" vert_align="c" complex_mode="1"/>
    </row_line>

    <row_now x="0" y="0" width="460" height="18">
        <text font="letterica16" r="255" g="255" b="255" align="l" vert_align="c" complex_mode="1"/>
    </row_now>

    <row_dim x="0" y="0" width="460" height="18">
        <text font="letterica16" r="140" g="140" b="140" align="l" vert_align="c" complex_mode="1"/>
    </row_dim>

    <row_gap x="0" y="0" width="460" height="12"/>
</w>
